n=zenity
v=3.12.1
u=http://ftp.gnome.org/pub/gnome/sources/$n/${v%.*}/$n-$v.tar.xz

Src() {

	./configure --prefix=$usr \
				--sysconfdir=$etc \
				--localstatedir=$var
	make
}

Pkg() {
	make DESTDIR=$pkg install
}