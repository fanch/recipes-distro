n=libllvm
v=3.4
u=http://llvm.org/releases/$v/llvm-$v.src.tar.gz
p=llvm-$v

Src() {
	CC=gcc CXX=g++ \
	./configure --prefix=/usr \
            	--sysconfdir=/etc \
            	--enable-libffi \
            	--enable-optimized \
            	--enable-shared \
            	--disable-assertions
	make tools-only
}

Pkg() {
	install -Dm755 Release/lib/libLLVM-$v.so $pkg/$lib/libLLVM-$v.so
}
